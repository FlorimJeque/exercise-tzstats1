import { ShortSenderPipe } from './short-sender.pipe';

describe('ShortSenderPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortSenderPipe();
    expect(pipe).toBeTruthy();
  });
});
